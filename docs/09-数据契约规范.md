# 数据契约规范

> 本文档是“书外之遇”前后端、数据库与 AI 协作的共同规则。任何功能实现前，先阅读本文档及其指向的类型、Schema 和 migration。

## 1. 契约的四个来源

| 来源 | 路径 | 作用 | 修改者 |
| --- | --- | --- | --- |
| 说明规则 | `docs/09-数据契约规范.md` | 字段含义、接口与变更流程 | 两人共同 |
| 编译期类型 | `lib/types/resources.ts` | 前后端代码使用同一字段形状 | 两人共同 |
| 运行期校验 | `lib/schemas/resources.ts` | API 拒绝不合法请求 | 杨思涵主责 |
| 数据库事实 | `supabase/migrations/20260821_create_library_foundation.sql` | 表、索引、RLS 与权限 | 杨思涵主责 |

冲突时的优先级：**已执行的 migration > Zod Schema > TypeScript 类型 > 文档说明**。发现不一致时先提契约变更，不允许在业务页面中临时补丁。

## 2. 文件结构

```text
docs/09-数据契约规范.md
lib/types/api.ts                  # 全项目统一成功/失败格式
lib/types/resources.ts            # Resource、Tag、收藏与资源接口类型
lib/schemas/resources.ts          # query/path/body 的 Zod Schema
lib/services/resource-catalog.ts  # 待实现：资源查询业务模块
lib/services/personal-library.ts  # 待实现：收藏业务模块
app/api/resources/route.ts        # 待实现：资源搜索接口
app/api/resources/[slug]/route.ts # 待实现：资源详情接口
app/api/saved-resources/route.ts  # 待实现：收藏列表与创建接口
supabase/migrations/20260821_create_library_foundation.sql
supabase/seed/resources-v1.sql    # 待创建：精选资源种子数据
```

## 3. 命名与序列化规则

| 场景 | 规则 | 示例 |
| --- | --- | --- |
| PostgreSQL 表和字段 | `snake_case` | `saved_resources`, `published_year` |
| TypeScript 与 API JSON | `camelCase` | `savedResources`, `publishedYear` |
| URL 资源标识 | 小写 kebab-case slug | `the-library-of-babel` |
| 数据库主键 | UUID | `id` |
| 时间字段 | 数据库存 `timestamptz`，API 返回 ISO 8601 字符串 | `2026-08-21T09:30:00.000Z` |
| 空值 | 不存在或未知的数据使用 `null`，不使用空字符串 | `coverUrl: null` |
| 枚举 | 只用预定义小写字符串，不临时新增 | `availability: "online"` |

数据库字段到 API 字段的映射由 `lib/services/` 负责，页面不得处理 `snake_case`。

## 4. 通用 API 规则

所有接口使用 `lib/types/api.ts` 的统一响应：

```ts
type ApiSuccess<T> = {
  data: T;
  requestId: string;
};

type ApiFailure = {
  data: null;
  error: { code: ApiErrorCode; message: string };
  requestId: string;
};
```

| 情况 | HTTP | 错误码 |
| --- | --- | --- |
| 参数不合法 | 400 | `VALIDATION_ERROR` 或 `INVALID_JSON` |
| 未登录 | 401 | `UNAUTHORIZED` |
| 无权限 | 403 | `FORBIDDEN` |
| 资源不存在 | 404 | `RESOURCE_NOT_FOUND` |
| 重复收藏 | 409 | `ALREADY_SAVED` |
| 服务配置/外部服务失败 | 503 | `CONFIGURATION_ERROR` 或 `SUPABASE_UNAVAILABLE` |
| 其他服务端异常 | 500 | `INTERNAL_ERROR` |

前端不解析 Supabase 原始错误，也不通过错误英文文本判断业务状态。

## 5. 资源领域标准

`Resource`、`Tag`、`ResourceRelation`、`SavedResource` 的唯一正式定义位于 [lib/types/resources.ts](../lib/types/resources.ts)。

### 允许的枚举值

```text
ResourceType: book | paper | talk | collection
Availability: available | online | reference_only | check_library
TagCategory: discipline | theme | format
RelationType: same_theme | contrasting_view | historical_context | unexpected_bridge
```

新增枚举值必须同时更新：TypeScript union、Zod enum、SQL `check` 约束、前端显示文案与本文档。

## 6. 接口标准

| 接口 | 参数 / Body | 成功返回 |
| --- | --- | --- |
| `GET /api/resources` | `q?`, `tag?`, `type?`, `limit?` | `SearchResourcesData` |
| `GET /api/resources/[slug]` | path `slug` | `GetResourceData` |
| `GET /api/saved-resources` | 无 | `SavedResource[]` |
| `POST /api/saved-resources` | `{ resourceId, note? }` | `SavedResource` |
| `DELETE /api/saved-resources/[resourceId]` | path `resourceId` | `{ resourceId }` |

每个 Route Handler 必须按以下顺序实现：解析输入 -> Zod 校验 -> 身份/归属检查 -> 调用 `lib/services/` -> 转换为 API 类型 -> 返回统一 JSON。

## 7. 权限规则

- 资源、标签和资源关系是公开可读数据；浏览器仍优先通过 Next.js API 读取，避免页面依赖数据库查询细节。
- `profiles` 和 `saved_resources` 必须开启 RLS。
- 用户只能读取、创建、删除自己的收藏；`saved_resources` 用 `(user_id, resource_id)` 主键防止重复收藏。
- `SUPABASE_SERVICE_ROLE_KEY` 仅限服务端 adapter 使用，绝不进入 Client Component、`NEXT_PUBLIC_` 变量、日志、文档样例或 Git。
- 任何写入用户数据的接口都必须在 API 层验证用户身份，即使数据库已经有 RLS。

## 8. AI 使用规则

当让 AI 编码时，使用以下前置指令：

```markdown
开始前必须阅读：
- docs/09-数据契约规范.md
- lib/types/api.ts
- lib/types/resources.ts
- lib/schemas/resources.ts
- supabase/migrations/20260821_create_library_foundation.sql

这些文件是数据规范来源。不得自行新增字段、枚举、表、接口返回格式或权限策略。
如果规范不满足需求，先输出“契约变更提议”：变更内容、影响文件、数据迁移、兼容性与验收方式；未确认前不要写业务实现。
```

## 9. 契约变更流程

1. 提出变更原因和用户价值。
2. 标注影响：TypeScript、Zod、migration、service、Route Handler、前端页面、种子数据、文档。
3. 两人确认后，先新增 migration，绝不修改已经在线执行过的旧 migration。
4. 再同步更新 Zod、TypeScript、service 与前端。
5. 在 PR 描述写出迁移执行顺序和回归步骤。
6. 通过 Preview 验证后才合并 `main`。

## 10. 执行步骤

1. 杨思涵在 Supabase SQL Editor 执行 `20260821_create_library_foundation.sql`。
2. 两人共同创建 `supabase/seed/resources-v1.sql`，先写 20 条资源。
3. 佟雨泽以 `ResourceListItem` Mock 实现首页/搜索/详情 UI。
4. 杨思涵实现资源查询 service 和两条只读 API。
5. API 接通后，佟雨泽只替换数据源，不改变页面数据形状。
6. 完成搜索和详情闭环后，再实施 Auth 与收藏。
